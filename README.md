# Cross Sector — V44

Home now uses one fixed viewport below the header. The document height acts only as a vertical scroll ruler; window scroll progress translates the horizontal track. This removes sticky/pin/touch-controller conflicts. Phone keeps desktop-like fixed column widths and hides the standalone hero panel; tablet keeps the hero and fixed columns.
